Make a precise minimal scientific diagram edit to the attached ObjDec motivation figure. Preserve the entire canvas, typography, all three panels, all photos and illustrations, overall layout and nearly all labels exactly. This is not a new redesign. Fix only these specific issues in panel (c), plus add a tiny symbol legend if it fits.

1. In panel (c), there are THREE stacked 'Input features' strips. The bypass currently descends from only the bottom strip, falsely suggesting only radar original features are preserved. Correct the wiring: the outputs of ALL THREE strips meet the common thin dark vertical collector just to their right; this same collector branches to BOTH Shared and Modality-specific lanes and ALSO extends downward into the existing bottom bypass routed into Object-guided fusion. Remove the existing dangling bottom-strip-only bypass origin. Use a small junction dot on the common collector before its downward branch. It must be visually unambiguous that the original input feature path includes all modalities. Keep the path thin and uncluttered, no crossing lane contents.
2. At the bottom of panel (c), keep the 'Object supervision (training only)' label, but replace the two dashed double-headed arrows with dashed, unarrowed leader lines that terminate at small circular loss markers immediately below the decoupling and fusion/control boxes. No upward arrowheads entering the boxes. This is TRAINING LOSS supervision, NOT an inference input. Tiny word 'Loss' at each marker is acceptable if readable.
3. On the far right, the car cuboid output has '3D detection' above it and 'BEV (fused)' below, which confuses the output with a feature map. Keep '3D detection' and remove the 'BEV (fused)' label. Keep the car cuboid as a conceptual detection illustration.
4. At the foot of panel (c), retain the bold takeaway 'Explicit representation roles guide fusion'. Preserve footer 'Conceptual illustration'.

Do not add any other text. Do not change the conceptual examples in the middle panel or the adaptive fusion block on the left. Preserve all white space, moderate pale colors, navy academic serif lettering and exact image size/aspect ratio. The output is still a conceptual motivation figure, not measured data.
