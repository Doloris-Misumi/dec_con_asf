Edit this diagram background, preserving panels (a) and (b) EXACTLY and keeping all observation slots EMPTY. Correct panel (c) layout only to enlarge the future real prediction image. Keep the existing single input token and its two independent PARALLEL projection branches exactly correct. Move the output rectangle from the small square at the far right to a WIDE LANDSCAPE blank rectangle in the lower-right area of panel c, at approximately x=1610, y=468, width=340, height=168 in this 1983x793 canvas. The rectangle must be pure white and will later receive real predictions; do not draw any sample. Its short title "3D detection" should sit just above. Connect the Object-guided fusion block downward then right into the top of this rectangle. Remove the old small output square, old title and old arrow completely, leaving white space there. Position a compact "GT: training only" and two small dashed-arrow loss boxes BELOW the branch projections and to the LEFT of the large output, without any overlap. Training-only diagram must have both GT and predictions enter Loss; no arrow from GT or loss into inference feature path. Preserve the residual input bypass into Object-guided fusion; it may be routed horizontally under branches at y=427. Keep shared c_m amber, specific u_m blue. Preserve all three takeaway strips, style, typography and colors. No other changes; in particular the single t_m must fork independently to Shared projection and Specific projection, not a serial path.
