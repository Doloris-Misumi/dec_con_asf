_BASE_CONFIG_: './configs/ASF_obj_patch_dec_gentle.yml'

GENERAL:
  NAME: 'FgGatedPatchDec_A2FUSION_rlc_l1d256_l2p2t32d256g_l2g_scl_mha16'

MODEL:
  FUSER:
    NAME: ForegroundGatedPatchDecA2Fusion
    PATCH_DEC_ENABLED: True
    PATCH_DEC_SELECTION: 'gt_foreground'
    PATCH_DEC_FG_MARGIN: 0.6
    PATCH_DEC_FG_FALLBACK_TO_ENERGY: False
    PATCH_DEC_TOPK_RATIO: 0.25
    PATCH_DEC_MIN_PATCHES: 512
    PATCH_DEC_HIDDEN_DIM: 256
    PATCH_DEC_RES_SCALE: 0.08
    PATCH_DEC_RES_SCALE_LEARNABLE: False
    PATCH_DEC_LAMBDA_DECOUPLE: 0.08
    PATCH_DEC_LAMBDA_COMMON: 0.08
    PATCH_DEC_LAMBDA_UNIQUE: 0.01
    PATCH_DEC_UNIQUE_MARGIN: 0.1
    PATCH_DEC_GATE_HIDDEN_DIM: 128
    PATCH_DEC_GATE_MIN: 0.0
    PATCH_DEC_GATE_MAX: 1.0
    PATCH_DEC_GATE_LOSS_WEIGHT: 0.05
    PATCH_DEC_GATE_POS_WEIGHT_MAX: 20.0

  LOSS:
    PATCH_DEC_WEIGHT: 0.1
